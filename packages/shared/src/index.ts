/**
 * @swipehire/shared — types shared between web, api, worker, and (occasionally) ml-sidecar.
 *
 * Avoid importing heavy runtime deps here. Zod schemas + types only.
 */

export * from './types/index.js';
